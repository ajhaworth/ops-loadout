# One plugin per line. Lines starting with # are ignored.
#   blender_org <extension id>     -> from extensions.blender.org
#   github <owner/repo>            -> latest release .zip asset (fallback: repo zipball)
#   forgejo <host/owner/repo>      -> latest release .zip asset from a Forgejo/Gitea host
# Examples:
# blender_org node_wrangler
# github sidefx/blender-houdini-engine
# official Blender Lab MCP add-on; install.sh step 6 registers its server with Claude Code
forgejo projects.blender.org/lab/blender_mcp
# core modeling set for environment work; Node Wrangler is still bundled with Blender, setup.py enables it
blender_org bool_tool
blender_org looptools
blender_org extra_mesh_objects
blender_org edit_mesh_tools
blender_org copy_attributes_menu
