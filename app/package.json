{
  "name": "launchbay",
  "private": true,
  "version": "0.3.1",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "bundle-repo": "node scripts/bundle-repo.js",
    "build": "tauri build",
    "install:mac": "tauri build --bundles app && rm -rf '/Applications/Launchbay.app' && ditto 'src-tauri/target/release/bundle/macos/Launchbay.app' '/Applications/Launchbay.app' && (pkill -x 'Launchbay' || true) && open '/Applications/Launchbay.app'",
    "install:win": "tauri build --bundles nsis"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.5"
  }
}
