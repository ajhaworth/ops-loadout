{
  "name": "ops-launcher",
  "private": true,
  "version": "0.2.2",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "bundle-repo": "node scripts/bundle-repo.js",
    "build": "tauri build",
    "install:mac": "tauri build --bundles app && rm -rf '/Applications/Ops Launcher.app' && ditto 'src-tauri/target/release/bundle/macos/Ops Launcher.app' '/Applications/Ops Launcher.app' && (pkill -x 'Ops Launcher' || true) && open '/Applications/Ops Launcher.app'",
    "install:win": "tauri build --bundles nsis"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.5"
  }
}
